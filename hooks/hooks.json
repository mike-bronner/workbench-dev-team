{
  "description": "Commit approval gate — every git commit requires explicit human approval. Carve-out: autonomous Index-pipeline runs, marked by WORKBENCH_DEV_TEAM_PIPELINE=1 on the process bin/dispatch-agent.sh spawns.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/commit-approval-gate.sh\""
          }
        ]
      }
    ]
  }
}
